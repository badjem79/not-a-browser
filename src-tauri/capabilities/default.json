{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the chrome webview (our app UI)",
  "windows": ["main"],
  "webviews": ["chrome"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
